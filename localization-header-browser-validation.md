# Localization header validation

On the local index page, the closed header control displayed only `EN` and its accessible label identified the current language and currency as English and US Dollar.

After pressing the button, the language and currency selectors appeared in a compact panel. The selectors exposed full language names and currency labels, while the closed button remained abbreviation-only. The panel was positioned in the header area rather than fixed at the bottom of the viewport.
