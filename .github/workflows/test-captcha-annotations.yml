name: Test CAPTCHA annotation workflow
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo CAPTCHA annotation workflow active
